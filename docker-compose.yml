version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: qizzle-mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: qizzle
      MYSQL_DATABASE: qizzle
      MYSQL_USER: qizzle
      MYSQL_PASSWORD: qizzle
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    command: --default-authentication-plugin=mysql_native_password

  adminer:
    image: adminer:latest
    container_name: qizzle-adminer
    restart: always
    ports:
      - "8080:8080"
    depends_on:
      - mysql

volumes:
  mysql_data:
